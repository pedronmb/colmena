<?php

declare(strict_types=1);

/**
 * Plantilla de configuración. Copiá este archivo a config.php y ajustá valores:
 *   copy config.php.default config.php   (Windows: copy config\config.php.default config\config.php)
 * config.php no se versiona (ver .gitignore).
 */
return [
    'db' => [
        'path' => dirname(__DIR__) . DIRECTORY_SEPARATOR . 'database' . DIRECTORY_SEPARATOR . 'app.sqlite',
    ],
    // Solo config.php (no variables de entorno). No subas config.php al repo si incluye el PAT.
    'azure_devops' => [
        'organization' => '',
        'project' => '',
        'pat' => '', // Personal Access Token (Work items: Read)
        'max_items' => 200,
        'wiql' => null, // null en sync usa WIQL de ítems activos (sin estados finales)
        'final_states' => null, // null = Complete, Done, Removed, Closed, Completed
        'reconcile_limit' => 50,
        'default_board' => 'develop',
        // Agrupa estados reales de Azure bajo el nombre de columna del tablero.
        'state_aliases' => [
            'Backlog / new' => ['Backlog', 'New'],
            'UAT' => ['En Prueba UAT'],
        ],
        'boards' => [
            'develop' => [
                'label' => 'Board Develop',
                'columns' => [
                    'Backlog / new',
                    'To Do',
                    'Blocked',
                    'In Progress',
                    'To Be Tested',
                    'Resolved',
                ],
            ],
            'testing' => [
                'label' => 'Board Testing',
                'columns' => [
                    'Backlog / new',
                    'To Be Tested',
                    'In Testing',
                    'Blocked',
                    'Resolved',
                    'UAT',
                ],
            ],
        ],
    ],
    // InvGate Service Desk (sync de tickets). No subas config.php si incluye la contraseña.
    'invgate' => [
        'server_url' => '', // ej. https://empresa.cloud.invgate.net (con https://, sin barra final)
        'user' => '',
        'password' => '',
        'limit' => 100,
    ],
    // Ollama local para recomendaciones IA de tickets InvGate.
    'ollama' => [
        'base_url' => 'http://localhost:11434',
        'model' => 'llama3.1',
        'timeout' => 120,
    ],
];
